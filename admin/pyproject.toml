[project]
name = "upsun-task-playground-admin"
version = "0.1.0"
description = "Admin UI for triggering the coding-agent task. FastAPI + uvicorn (ASGI). See ITERATION-2.md."
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "jinja2>=3.1",
    "python-multipart>=0.0.20",
    "itsdangerous>=2.2",
    "argon2-cffi>=23.1",
    "httpx>=0.27",
]
