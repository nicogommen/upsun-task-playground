[project]
name = "upsun-task-playground-agent"
version = "0.1.0"
description = "Agent task container for the Upsun task playground."
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.55.0",
]
